import 'package:flutter/material.dart';

class BottomBarTabs extends StatelessWidget {
  const BottomBarTabs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
