import 'package:flutter/material.dart';

class MoreProductsTab extends StatelessWidget {
  const MoreProductsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
