import 'package:flutter/material.dart';

class VenderInfoTab extends StatelessWidget {
  const VenderInfoTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
